machine:
  node:
    version: 6.5.0

test:
  override:
    - env

deployment:
  release:
    tag: /v[0-9]+\.[0-9]+\.[0-9]+/
    commands:
      - env

experimental:
  notify:
    branches:
      only:
        - master
