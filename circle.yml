machine:
  node:
    version: 6.5.0

test:
  override:
    - if [ -z "$CIRCLE_TAG" ]; then
        echo "bar"
        env
      else
        echo "foo"
      fi

deployment:
  release:
    tag: /^v\d+\.\d+\.\d+$/
    owner: zertosh
    commands:
      - env

experimental:
  notify:
    branches:
      only:
        - master
