machine:
  node:
    version: 6.5.0

test:
  override:
    - env

deployment:
  release:
    tag: /^v\d+\.\d+\.\d+$/
    owner: facebook
    commands:
      - env

experimental:
  notify:
    branches:
      only:
        - master
